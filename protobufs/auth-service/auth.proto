syntax = "proto3";

package auth;

service AuthService {
    rpc Verify(GenerateTokensDto) returns (User)
}

message GenerateTokensDto {
    string mail = 1;
    string password = 2;
}

message Tokens {
    string accessToken = 1;
    string refreshToken = 2;
}
